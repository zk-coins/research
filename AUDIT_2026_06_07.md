# Repo-Hygiene Re-Audit — 2026-06-07

Second-pass audit across the three repos on their current `develop`, against the
target model:

- **research** — catch-all: design, research, notes, papers, spikes, benchmarks. Anything goes.
- **docs** — *only* the target-design implementation of the current spec, consistent with the
  defined specs (not the running software). Docusaurus site `docs.zkcoins.com`.
- **node** — only what a professional software repo has (code, build, standard files); **no**
  design/research markdowns, session memos, roadmaps, spike crates, or benchmark output.

Method: `git fetch` + inventory of `origin/develop` per repo (top-level `*.md` + sizes, top-level
tree, recursive `*.md`); cross-checks against the model; stale-reference / broken-link sweeps
(`git grep` for moved filenames, relative markdown links, docs sidebar ids); duplicate check in
research. Audited tips: research `829f9bf`, node `d2247ba`, docs `ed7fdec`.

## Inventory summary

### node — CLEAN (one finding)
Top-level: `Cargo.toml`/`Cargo.lock`/`Dockerfile`/`rust-toolchain`/`LICENSE`/`SECURITY.md` +
`README.md` (37 KB) + slimmed `CONTRIBUTING.md` (15 KB); dirs `node/ shared/ program-plonky2/
script-plonky2/ scripts/ .github/ .githooks/ .config/`. No design/research markdowns, no
session memos, no roadmap, no spike crate (the Plonky3 spike never reached develop). The only
non-standard markdown are crate dev-guides (`program-plonky2/CONTRIBUTING.md`,
`script-plonky2/CONTRIBUTING.md`) and CI/build docs (`scripts/ci-runner/README.md`) — all
legitimately code/build-adjacent. **Finding F1** below: `scripts/bench/results/` is the one
piece of pure benchmark *output* still in node.

### docs — mostly clean (one substantive consistency finding)
Top-level standard files + `docs/`, `src/`, `static/`, `plugins/`, `papers-typst/`,
`docusaurus.config.js`, `sidebars.js`, `package*.json`. The doc set is spec/architecture
content: `specification.md` (279 KB), `implementation-mandate.md`, `requirements.md`,
`protocol.md`, `architecture/*` (13 pages), `comparisons.md`, `tech-decisions.md`, `wallet.md`,
`risks.md`, `intro.md`, `roadmap.md`. `papers-typst/` is the **Typst pipeline that renders
`docs/specification.md` into a spec PDF** (confirmed by its README) — correctly docs-scoped, not
research. Research/infrastructure removal from round 1 is complete (no `docs/research/`,
`docs/infrastructure/`, or `static/research/`; stale-ref sweep clean). **Finding F4** below: the
`architecture/*` pages (and `roadmap.md`) describe a model the spec now supersedes.

### research — CLEAN, correctly organized
All round-1 moves are under the right paths: `zkcoins-design/` (design drafts + `SPEC.md` +
`ROADMAP.md` + `plonky3-migration/` + `program-plonky2-sessions/`), `benchmarks/` (Plonky3
write-ups), `spikes/plonky3-recursion-spike/` (the crate), `community-content/` (docs research
pages), `operations/` (docs infrastructure pages). Pre-existing research stays at root
(`formal/`, `notes/`, `primary-sources/`, `papers-typst/`, `applications/`, `upstream/`, the
paper PDFs, `bitvm-bridge-research.md`, `coinbase-wallet-analysis.md`, `PROTOCOL_STATUS.md`). No
duplicates, no strays (every plonky3/design artifact lives under `zkcoins-design|benchmarks|
spikes`). Maintainer additions since the merge (`formal/concept-review/`) are correctly research.
This PR adds `benchmarks/node-runtime/` (see F1) and this report.

## Findings

| # | Severity | Repo | Path | Why it violates the model | Action |
|---|---|---|---|---|---|
| **F1** | medium | node | `scripts/bench/results/` (README + 1 CSV + 1 JSON + `m5-max-vs-m3-ultra-2026-06-02.md`) | Benchmark **output** — model says node carries no bench-output. The harness (`node/src/bin/probe_r2.rs`) is code and stays; only the data is misplaced. Nothing references the dir externally. | **Fixed.** Archived → `research/benchmarks/node-runtime/` (this PR); removed from node in [zk-coins/node#219](https://github.com/zk-coins/node/pull/219). |
| **F2** | low | node | `.github/workflows/ci.yaml` (~L113), `scripts/ci-runner/README.md` (~L12) | Stale reference to the CONTRIBUTING section *"Working on the Plonky2 Migration"*, removed when CONTRIBUTING was slimmed in [#216](https://github.com/zk-coins/node/pull/216). Develop-specific. | **Fixed** in [#219](https://github.com/zk-coins/node/pull/219): repointed to § "Hardware target". |
| **F3** | low (accepted) | node | `program-plonky2/src/{lib,hash,types}.rs`, `program-plonky2/src/circuit/{main,source_aggregator,recursion_shape_probe}.rs`, `node/src/{account_node,db,runtime}.rs` | ~14 **code-comment** citations of `MIGRATION_RESEARCH.md` / `SPEC.md` (now in research). These are backtick prose in `//!`/`///`/`//` comments — **not** hyperlinks, so no 404 and no build impact. `program-plonky2/CONTRIBUTING.md` already carries a provenance note pointing readers to `zk-coins/research/zkcoins-design/`. | **Documented, left as-is.** Repointing rationale comments in compiled source is disproportionate churn for zero functional benefit; the provenance note covers discoverability. Batch-repoint later only if desired. |
| **F4** | **substantial — USER DECISION** | docs | `docs/architecture/*` (esp. `overview.md`, `nullifier-design.md`, `trust-model.md`, `information-flow.md`, `proof-system.md`) and `docs/roadmap.md` | These describe the **superseded** model — SMT+MMR commitment history, one recursive proof per send, HTTP delivery. The repo's own [`docs/implementation-mandate.md`](https://docs.zkcoins.com/implementation-mandate) §1 declares that model **non-authoritative**: the authoritative spec (`docs/specification.md`) is the **batched** design — off-chain `SpendRecord`s aggregated by permissionless **publishers** into `BatchBundle`s, `AggregateBatchProof`, constant-size `BatchInscription`, Nostr + Blossom transport. So docs currently documents two eras side by side, violating "consistent to the defined specs". `roadmap.md` is a *Plonky2-migration* tracker, which the green-field mandate now treats as prototype history. | **Report only — not auto-fixed.** Reconciling the architecture pages to the batched spec (or relabeling them as superseded/impl-status) is an architecture/content task, not janitorial. Needs a maintainer decision: (a) rewrite `architecture/*` to the batched model, (b) prepend a "superseded — see specification.md" banner to the stale pages, or (c) move the old-model pages to research and keep only spec-consistent architecture. Same question for whether `roadmap.md` should stay on a green-field docs site. |

### Earlier-known consistency items (carried from round 1, subset of F4)
The PR-C body already flagged, as TODOs, that `architecture/*` tag P02 (queryable nullifier
accumulator) and P06 (receive-side recursive re-verification) as "roadmap/not today", that
`requirements.md #2` over-claims P05 on-chain-privacy, and stale data-structure claims
(`overview.md` still lists "Merkle Mt. Range"; `nullifier-design.md` "old subtrees can be
pruned" contradicts `specification.md §3.7`). These all roll up into **F4**.

## Disposition

- **F1, F2** — fixed via two coordinated draft PRs (this research intake + [zk-coins/node#219](https://github.com/zk-coins/node/pull/219)); recommended merge order: research first.
- **F3** — documented, intentionally left (covered by provenance note; not a broken link).
- **F4** — documented for maintainer decision; **not** auto-fixed (substantial, architecture-level).

No HARD-rule violations encountered. No self-merge, no deploy, no force-push performed.

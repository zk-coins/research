================================================================================
zkCoins 100% Logical Verification Initiative
CERTIFICATE -- P03 Per-Asset Balance Conservation
================================================================================

Property      : P03 Per-Asset Balance Conservation.
Claim         : For every asset `a`, the live supply of `a` (total value of all
                unspent coins of `a` held across every account) never exceeds the
                cumulative Mint(a) produced by valid v1 issuance transitions.
                Outputs never exceed inputs plus an explicit, predicate-checked,
                creator-bound Mint; held value is never created except by a Mint
                that the compliance predicate C (Sec. 2.1 clause 3 + the Sec. 6.5
                v1 clauses) authorises.  (Pass-3 audit P3, HIGH.)
                NOTE: v1 supply is CREATOR-BOUND, not protocol-capped (Sec. 6.5:
                "supply discipline is a creator's commitment, not a protocol
                guarantee"). P3 is the no-inflation inequality, NOT a ceiling.
Result        : VERIFIED -- unbounded, via inductive invariant over the
                per-account compliance state machine module/Proofs.tla.
                spec-v1.1 (docs#47 §3.8) FEE-COIN ATOMICITY additionally
                VERIFIED unbounded in fee_atomicity.tla: the fee coin reaches
                `completed` IFF the recipient payment under the same ocr does
                (atomic), and a never-anchored fee is never spendable
                (censorship-safe). Conservation itself is UNCHANGED by docs#47
                (diff-confirm: the fee coin is just another output under ocr).

Tool          : Apalache 0.58.0  (build 711dce6; SMT backend: Z3 4.14.1.0)
Host          : Apple Silicon (arm64), macOS (Darwin 25.5.0)
Date          : 2026-06-07
Spec baseline : zk-coins/docs@ed7fdece (spec-v1.1 = b6972b8 + docs#46/#47/#48;
                Proofs Sec. 2.1 clause 3, Architecture Sec. 6.5 v1 mint clauses,
                Sec. 6.6 capability row 3; fee coin On-chain §3.8)
Models        : property.tla        (module `property`, EXTENDS module/Proofs.tla)
                fee_atomicity.tla    (module `fee_atomicity`, spec-v1.1; self-contained)
Runner        : ./verify.sh    (stages Foundations+Assumptions+Adversary+Proofs;
                fee_atomicity self-contained)

--------------------------------------------------------------------------------
WHAT IS PROVEN
--------------------------------------------------------------------------------
property.tla EXTENDS the full per-account compliance machine module/Proofs.tla:
the nine-clause compliance predicate C (Sec. 2.1), the v1 InitialProof mint and
AccountUpdateProof send actions, the A7 signing oracle, and the per-account,
per-asset balance bookkeeping. Every transition is admitted ONLY when C holds,
so Sec. 2.1 clause 3 (In(a) + Mint(a) >= Out(a)) is enforced on every step by
construction.

P3 quantifies live supply against TOTAL MINTED -- a history-dependent quantity
that is NOT recoverable from the machine's state (a coin's value is private
witness data, Sec. 2.1 clause 9; the per-account coin-history set stores coin
IDs, not amounts). A property module may not redefine PrNext's assignments, so
this module defines its OWN thin state machine that:
  (i)   reuses every Proofs datatype, helper, and the predicate C verbatim;
  (ii)  drives the SAME two C-gated actions with the SAME prState / prChSet /
        prAuthorised updates as Proofs.MintStep / Proofs.SendStep;
  (iii) adds two GHOST ledgers, both per asset:
          prSupply[a] -- live coin value (sum of unspent coin amounts);
          prMinted[a] -- cumulative Mint(a) over admitted v1 issuances.
        A mint raises BOTH by exactly the issued amount; a self-conserving send
        (In(a) = Out(a)) leaves both unchanged. The ghosts are observational
        only -- they never gate a transition -- so the reachable prState behaviour
        is identical to Proofs'; they only make the mint history observable so
        conservation can be stated and proven inductively.

Conservation invariant:  \A a \in AssetCarrier : prSupply[a] <= prMinted[a].

The unbounded proof is the standard inductive-invariant argument [2]+[3]+[4]:
  [2] base        P3Init  => IndInv
  [3] step        IndInv /\ P3Next => IndInv'
  [4] implication IndInv  => Conservation
=> Conservation holds in EVERY reachable state, for an arbitrary number of mint
   and send transitions. [1] is a supporting bounded reachability sanity pass.

IndInv == PrTypeOK /\ MintedTypeOK /\ Structural /\ Conservation, where:
  - PrTypeOK    -- the EXACT typing predicate of module/Proofs.tla (shared);
  - MintedTypeOK-- both ghost ledgers total over the asset carrier and >= 0;
  - Structural  -- owner = a always (clause 7 never changes it), and a
                   send_counter = 0 account still has current_pubkey = a, so the
                   only mint it can perform creates an asset whose creator a is
                   in Accounts (in AssetCarrier). This reachability fact is what
                   PrTypeOK alone does not give and is what closes the induction
                   (see notes.md strengthening log).

--------------------------------------------------------------------------------
spec-v1.1 (docs#47 §3.8) -- FEE-COIN ATOMICITY (fee_atomicity.tla)
--------------------------------------------------------------------------------
docs#47 §3.8 adds the publisher fee coin: one ordinary output addressed to the
publisher's fee_address, under the transition's SINGLE output_coins_root (ocr).
Conservation is UNCHANGED (diff-confirm): the fee coin is just another member of
OutputIds / addend of OutAmount, so clause 3 and the clause-6 ocr binding already
cover it. The genuinely NEW guarantee -- previously hand-argued -- is ATOMICITY:
anchoring the one SpendRecord admits ALL outputs under that ocr, or NONE.

fee_atomicity.tla models the §3.8 binding over ocr ids (A16: an ocr IS its output
set, with BOTH the payment and the fee under that ONE set), reusing the P02
accumulator-lifecycle idiom: Admit moves a whole ocr into `pending`, Confirm
promotes the whole ocr to `completed` (>= 6 conf), Reorg reverts only `pending`
(A12). A coin's completed/spendable status is DERIVED from its ocr's zone, never
set per-output -- that derivation IS the atomic binding. Proven UNBOUNDED:
  [F2] base   Init => IndInv     (IndInv = TypeOK /\ ZonesDisjoint /\ FeeAtomicity)
  [F3] step   IndInv /\ Next => IndInv'
  [F4] impl   IndInv => FeeAtomicity
where FeeAtomicity == \A o \in Ocrs :
        (FeeCompleted(o) <=> PaymentCompleted(o))          (a) atomic
     /\ (~ Anchored(o) => ~ FeeCompleted(o))               (b) censorship-safe
=> in every reachable state, any number of transitions, the fee and the payment
   under one ocr are admitted/completed together, and a never-anchored fee is
   never spendable. [F1] bounded sanity (length 6) supports; [F5] is a vacuity
   probe (NoneEverCompleted returns a counterexample => `completed` is reachable,
   so the IFF is exercised over a populated state, not vacuously).

--------------------------------------------------------------------------------
"UNBOUNDED" -- honest scope
--------------------------------------------------------------------------------
Checks [2]+[3]+[4] remove any bound on the NUMBER of transitions: the inductive
step is a single SMT query over one arbitrary IndInv-state and one P3Next step.
The account/asset universe is a finite parameter (ConstInit: Accounts = {1,2},
one v1 asset family); the inductive argument is per-asset, per-account local --
every IndInv conjunct quantifies per asset / per account and each action touches
one account (and, for a mint, one asset), so the argument is uniform in the
universe size (the structural warrant for generality; see notes.md).
Data-domain finiteness per state (amounts bounded by U128Bound) is intrinsic to
SMT-based model checking and is a modelling parameter, not a bound on the number
of transitions. The inductive-step start state is assigned with Apalache!Gen and
constrained by IndInv (the natural set-of-functions assignment over rich account
states triggers Apalache's "expand a set of functions" guard; Gen is the
sanctioned idiom -- see notes.md).

--------------------------------------------------------------------------------
COMMANDS (exactly as run by verify.sh)
--------------------------------------------------------------------------------
  apalache-mc check --cinit=ConstInit --init=P3Init    --next=P3Next --inv=IndInv       --length=6  property.tla  # [1] bounded sanity
  apalache-mc check --cinit=ConstInit --init=P3Init    --next=P3Next --inv=IndInv       --length=0  property.tla  # [2] inductive base
  apalache-mc check --cinit=ConstInit --init=IndInvInit --next=P3Next --inv=IndInv       --length=1  property.tla  # [3] inductive step
  apalache-mc check --cinit=ConstInit --init=IndInvInit --next=P3Next --inv=Conservation --length=0  property.tla  # [4] implication

Fee atomicity (fee_atomicity.tla, self-contained; Ocrs = 1..3 via ConstInit):
  apalache-mc check --cinit=ConstInit --init=Init       --next=Next --inv=IndInv            --length=6  fee_atomicity.tla  # [F1] bounded sanity
  apalache-mc check --cinit=ConstInit --init=Init       --next=Next --inv=IndInv            --length=0  fee_atomicity.tla  # [F2] base
  apalache-mc check --cinit=ConstInit --init=IndInvInit --next=Next --inv=IndInv            --length=1  fee_atomicity.tla  # [F3] step
  apalache-mc check --cinit=ConstInit --init=IndInvInit             --inv=FeeAtomicity      --length=0  fee_atomicity.tla  # [F4] implication
  apalache-mc check --cinit=ConstInit --init=Init       --next=Next --inv=NoneEverCompleted --length=3  fee_atomicity.tla     # [F5] vacuity probe (expect Error)
  apalache-mc check --cinit=ConstInit --init=Init       --next=Next --inv=FeeAtomicity      --length=3  fee_atomicity_nc.tla  # [F6] negative control (expect Error)

(EXTENDS resolution: verify.sh stages Foundations.tla, Assumptions.tla,
 Adversary.tla, Proofs.tla and property.tla into a temp dir and runs there, so
 the module sources stay single-copy under formal/module/. fee_atomicity.tla is
 self-contained and runs from the property directory.)

--------------------------------------------------------------------------------
TRANSCRIPT -- ./verify.sh
--------------------------------------------------------------------------------
Apalache version: 0.58.0
Instance: Accounts = {1,2}, one v1 asset family (ConstInit).
Inductive argument is uniform in the account/asset universe (notes.md).

### [1] bounded sanity  P3Init/P3Next |= IndInv (length 6)
>>> [1] bounded sanity  P3Init/P3Next |= IndInv (length 6): OK

### [2] inductive BASE   P3Init => IndInv (length 0)
>>> [2] inductive BASE   P3Init => IndInv (length 0): OK

### [3] inductive STEP   IndInv /\ P3Next => IndInv' (length 1)
>>> [3] inductive STEP   IndInv /\ P3Next => IndInv' (length 1): OK

### [4] IMPLICATION      IndInv => Conservation (length 0)
>>> [4] IMPLICATION      IndInv => Conservation (length 0): OK

==================================================================
FEE ATOMICITY -- fee_atomicity.tla (spec-v1.1 / docs#47 §3.8, unbounded)
==================================================================
### [F1] bounded sanity  Init/Next |= IndInv (length 6)
>>> [F1] bounded sanity  Init/Next |= IndInv (length 6): OK

### [F2] inductive BASE   Init => IndInv (length 0)
>>> [F2] inductive BASE   Init => IndInv (length 0): OK

### [F3] inductive STEP   IndInv /\ Next => IndInv' (length 1)
>>> [F3] inductive STEP   IndInv /\ Next => IndInv' (length 1): OK

### [F4] IMPLICATION      IndInv => FeeAtomicity (length 0)
>>> [F4] IMPLICATION      IndInv => FeeAtomicity (length 0): OK

### [F5] VACUITY probe    NoneEverCompleted reachable-false (completed populated)
>>> [F5] VACUITY probe    NoneEverCompleted reachable-false (completed populated): OK (counterexample found, as required)

==================================================================
RESULT: 9 check(s) passed, 0 unexpected.
P03 BalanceConservation: VERIFIED (unbounded, inductive).
  Unbounded proof = [2] base + [3] step + [4] implication.
  [1] is a supporting bounded reachability sanity pass.
  Fee atomicity (docs#47 §3.8): unbounded [F2]+[F3]+[F4];
  bounded sanity [F1] + vacuity probe [F5]
  (negative control recorded in notes.md).

--------------------------------------------------------------------------------
NEGATIVE CONTROL -- clause 3 is load-bearing
--------------------------------------------------------------------------------
In a /tmp staging copy, the per-asset conservation conjunct of
Proofs.Clause3_Balance was DELETED:

    \A a \in AssetsTouched(w) : InAmount(w.inputs,a) + MintAmount(w,a)
                                  >= OutAmount(w.templates,a)        <-- removed

and an INFLATING send action was added to the property machine: it spends one
held coin of `amount` but emits a change coin of `amount + extra` (extra > 0) of
the same asset (Out(a) > In(a), Mint(a) = 0), with the ghost prSupply truthfully
raised by `extra`. Honestly this transition is rejected by C; with clause 3
deleted, C admits it. Running:

  apalache-mc check --cinit=ConstInit --init=P3Init --next=P3Next \
                    --inv=Conservation --length=4 property.tla

  # APALACHE version: 0.58.0 | build: 711dce6
    > Found 1 initializing transitions
    > Found 3 transitions
  State 2: state invariant 0 violated.
  Found 1 error(s)
  The outcome is: Error
  Total time: 3.233 sec

Counterexample (per-asset ghost values, asset = the v1 family of account 2):
  State0:  prSupply = 0,  prMinted = 0        (initial)
  State1:  prSupply = 1,  prMinted = 1        (account 2 mints 1)
  State2:  prSupply = 2,  prMinted = 1        (inflating send: spend 1, emit 2)
           => Conservation  prSupply[a] <= prMinted[a]  violated (2 > 1):
              one unit of asset created out of thin air.

So a NoError verdict WITH clause 3 is meaningful: the model can see inflation,
and the conservation conjunct of C is exactly what rules it out.

--------------------------------------------------------------------------------
NEGATIVE CONTROL -- the single-ocr atomic binding is load-bearing (fee coin)
--------------------------------------------------------------------------------
The committed companion fee_atomicity_nc.tla DECOUPLES the fee from the payment's
ocr: independent fee zones feePending/feeCompleted advanced by their OWN
AdmitFee/ConfirmFee, with FeeCompleted(o) reading feeCompleted instead of the
payment's `completed`. Running:

  apalache-mc check --cinit=ConstInit --init=Init --next=Next \
                    --inv=FeeAtomicity --length=2 fee_atomicity_nc.tla

  State 1: ... Admit    pending = {1}      (payment ocr 1 anchored)
  State 2: ... Confirm  completed = {1}, feeCompleted = {}
  The outcome is: Error                                        (exit 12)
  => FeeCompleted(1)=FALSE but PaymentCompleted(1)=TRUE: the decoupled fee can
     lag (or, via AdmitFee/ConfirmFee, race ahead of) the payment => FeeAtomicity
     (the IFF and the censorship clause) violated.

So once the fee is admitted independently of the payment's single ocr, atomicity
fails -- the definitive proof the all-or-none single-ocr binding is load-bearing.
The committed fee_atomicity.tla is unchanged; the negative-control _apalache-out
was discarded.

--------------------------------------------------------------------------------
CROSS-CHECK vs Pass-3 audit (Phase-4 input)
--------------------------------------------------------------------------------
  Pass-3 P3 label : HIGH (for the design as stated -- creator-bound supply).
  Apalache verdict: VERIFIED (unbounded) at the compliance-machine level.
  Reconciliation  : CONFIRMED. The formalised invariant is the audit's frame
                    "no inflation of others' assets" (Sec. 6.6 row 3), the
                    inequality prSupply <= prMinted -- NOT a supply ceiling,
                    which v1 deliberately omits (Sec. 6.5). The audit's HIGH is
                    conditional on A1/A3/A6/A7 plus the explicit creator-bound
                    model; this proof discharges the clause-3 conservation
                    bookkeeping over the machine, with those axioms carried as
                    the Assumptions oracles (A7 = the prAuthorised signing
                    oracle; A3/A16 = the structured-digest abstraction).

--------------------------------------------------------------------------------
SCOPE
--------------------------------------------------------------------------------
Verified: per-asset conservation as the clause-3 bookkeeping discipline over the
per-account compliance machine -- live supply is backed by authorised mint, no
non-creator inflation, no out-of-carrier asset minting.

Deliberately out of frame (matching the audit, by design): a protocol supply
CAP (none in v1); field-arithmetic non-wrap of u128 sums (an implementation-
review item per Pass-3 -- the spec mandates the in-circuit range check, modelled
here by U128Bound non-wrap); creator equivocation / parallel mint histories
(publicly observable on Bitcoin, not protocol-prevented, per Sec. 6.5). The A7
signature unforgeability and A3/A16 collision/commitment properties are carried
as Assumptions oracles, not re-proven here.
================================================================================
